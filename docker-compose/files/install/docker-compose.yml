# Generated by the anesis `docker-compose` addon.
# Add services with `anesis use docker-compose add-<service>` (postgres, redis, ...).
# The markers below are injection points — leave them in place.
#
# Each service below publishes its port to the host (e.g. 5432:5432) and
# ships with a default/placeholder credential (postgres/postgres, root/root,
# guest/guest, ...) so it works out of the box for local dev. That is not
# safe on any host reachable from outside your machine — set real
# credentials via .env (already wired up per service) and drop the host
# port mapping, or bind it to 127.0.0.1 (e.g. "127.0.0.1:5432:5432"), before
# running this anywhere but localhost.
services:
  # anesis:compose-services

volumes:
  # anesis:compose-volumes
